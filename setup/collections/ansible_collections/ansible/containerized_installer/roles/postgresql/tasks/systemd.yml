---
- name: Ensure automation-reporter-postgresql container is enabled and started
  ansible.builtin.systemd:
    name: automation-reporter-postgresql.service
    state: started
    scope: user
    daemon_reload: true
    enabled: true
  ignore_errors: 1

- name: Ensure automation-reporter-postgresql container is enabled and started
  ansible.builtin.systemd:
    name: automation-reporter-postgresql.service
    state: started
    scope: user
    enabled: true
  retries: 5
  delay: 5
...
