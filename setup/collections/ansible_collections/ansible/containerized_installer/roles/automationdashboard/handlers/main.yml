---
# - name: Restart dashboard rsyslog
#   ansible.builtin.systemd:
#     name: automation-dashboard-rsyslog.service
#     scope: user
#     state: restarted

- name: Restart dashboard task
  ansible.builtin.systemd:
    name: automation-dashboard-task.service
    scope: user
    state: restarted

- name: Restart dashboard web
  ansible.builtin.systemd:
    name: automation-dashboard-web.service
    scope: user
    state: restarted
...
