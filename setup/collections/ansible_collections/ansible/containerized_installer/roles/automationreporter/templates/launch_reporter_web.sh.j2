#!/usr/bin/env bash

/code/src/backend/wait-for-migrations-reporter.sh

# exec supervisord -c /etc/supervisord_task.conf
nginx -g 'daemon off;' &
/venv/bin/python manage.py runserver 0.0.0.0:8000 &
exec sleep inf
