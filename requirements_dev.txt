# Development and testing dependencies
# These are only used in CI and local development, not in production builds
pytest-cov==7.0.0
pytest-django==4.11.1
pytest-mock==3.15.1
pytest-responses==0.5.1
pytest==9.0.0
responses==0.25.8
time-machine==2.19.0
